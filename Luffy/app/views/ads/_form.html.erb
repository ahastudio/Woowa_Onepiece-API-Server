<%= form_for(ad) do |f| %>
  <% if ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ad.errors.count, "error") %> prohibited this ad from being saved:</h2>

      <ul>
      <% ad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ad_cd_id %>
    <%= f.number_field :ad_cd_id %>
  </div>

  <div class="field">
    <%= f.label :ad_title %>
    <%= f.text_area :ad_title %>
  </div>

  <div class="field">
    <%= f.label :ad_start_dt %>
    <%= f.date_select :ad_start_dt %>
  </div>

  <div class="field">
    <%= f.label :ad_end_dt %>
    <%= f.date_select :ad_end_dt %>
  </div>

  <div class="field">
    <%= f.label :bus_stop_id %>
    <%= f.text_field :bus_stop_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
